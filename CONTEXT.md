# CONTEXT — 蘑菇房相机巡检系统

领域词汇表。工程技能（架构评审、grilling、issue 编写）在命名输出时应使用这些术语。
完整设计见 `.scratch/fmc4030-camera-scan/spec.md`。

## 术语

| 术语 | 含义 |
| --- | --- |
| 库房（菇房） | 蘑菇种植库房；高湿环境，相机与导轨由拖链 + 防护罩防护 |
| 框（box） | 库房内的一个栽培框，测量的基本归属单位（`box_id`，如 `B105` = 第 1 层第 5 框） |
| 站位（station） | 一个框的一个拍照位，对应一组 `(y, z)` 坐标（`station_id`，如 `S105`）；当前每框 1 个站位 |
| 货架网格（rack grid） | 现场布局：横向 `GRID_COLS` 框 × 竖向 `GRID_LAYERS` 层（当前 **12 × 5 = 60**）；坐标由行程均分**推导**而非逐点手写，改框数只改这两个常量（`patrol.stations.build_grid`） |
| 层（layer） | 竖向第几层蘑菇架，1…`GRID_LAYERS`，**自顶向下**（第 1 层贴近 Z 原点，z 最大）；`Station.layer` |
| 框序号（col） | 横向第几框，1…`GRID_COLS`，沿 Y 递增（最左为 1）；`Station.col` |
| 蛇形遍历（snake order） | 站位生成/巡检顺序：层内单方向走完、相邻层反向，使换层只在同侧落一格，**横向空程为 0**（省掉每层约 4.1m 折返） |
| 巡检轮次（patrol round） | 依次经过全部站位采图的一轮作业；回零开始、回原位结束（`PatrolRound`） |
| 角度档（angle profile） | 顶拍 0°（量菌盖直径）/ 侧斜 45°（量菇体长度）；**当前每框 1 档（斜拍 45°）**，Z 行程已全部让给选层，改 `GRID_ANGLES` 即可恢复双档 |
| 补光窗口（lamp window） | OUT0 补光灯点亮的时段，即相机曝光窗口；M1 由采图返回决定，M2 为固定灯窗 |
| M0 / M1 / M2 | 三种运行模式：手动调试 / 主机调度巡检（主模式）/ 控制器脱机 .elo 脚本兜底 |
| 环控系统 | 库房现有温湿度/CO₂/光照系统，环境数据来源（接口形式待现场确认） |
| 最佳控制参数区间（ranges） | 分阶段生长速率 × 环境分箱分析产出的参数上下限表（`ranges` 表） |
| outbox | 主机本地待同步 JSONL 队列；at-least-once 推送 prod（ADR-0001） |
| 运动参数（motion profile） | 巡检/接近/回零速度与衰减等物理参数，M1/M2 单源（ADR-0002）；按轴拆分（ADR-0007） |
| Y 轴 | 水平长行程轴（轴号 1，行程 0…4492 mm，向右为正）；负限位回零落在左端 |
| Z 轴 | 竖直短行程轴（轴号 2，行程 -212…0 mm，向上为正）；正限位回零落在顶端 |
| 原点 | Y 反向（负限位）回零、Z 向上（正限位）回零的落点，即虚拟坐标 `(0, 0)`（ADR-0007） |
| 虚拟坐标（virtual coordinate） | `Line_2Axis` 的坐标口径，本机只有 `(y, z)` 两维、顺序恒为 Y 在前（本机不接 X 轴） |
| 轴号（axis index） | 控制器轴编号：0=X、1=Y、2=Z；`axis_mask = Σ(1 << index)`，本机 Y+Z = `0x06`（ADR-0007） |
| 合成速度（composite limit） | 两轴插补的速度/加减速由方向余弦折算 `v_comp = min_i(v_i / \|cos θ_i\|)`，使任一轴不越自身上限（ADR-0007） |
| 回零方向（home dir） | SDK `homeDir`：1=正限位、2=负限位（`.elo`「设置回零运动参数」参数 4） |
| 控制器整定参数（device para） | 控制器内的导程 / 细分 / **软限位** / 回零超时；`patrol.fmc.device_para` 读写，巡检启动前必须 `check_soft_limits()`——出厂默认软限位仅 ±200mm，会把长行程目标静默截断（ADR-0008） |
| 回零超时（home overtime） | 控制器在 `homeTime` 内没触发限位开关即自行终止回零并置位；此时点位**不是原点**，须抛 `HomeTimeoutError`（ADR-0008） |
| 滑块（carriage） | 双轴导轨上承载相机的运动部件（勿用"滑台"；模组是轴，滑块是轴上动的那个，两者不可混用） |
| 模组（module） | 一根直线运动模组本体，即**一个轴**：本机 Y 模组（水平长行程）+ Z 模组（竖直短行程）= **双模组**。用户口语里的"双模组上位置"就是该轴的 `(y, z)` 坐标（2026-09-14 确认） |
| 巡检台（console） | 新增部署单元：库房主机上向操作者提供实时/历史两种模式的界面（ADR-0003） |
| 实时模式（realtime） | 巡检台的手动模式：操作者直接指挥导轨运动与单次采图，仅在 M0 语义下生效（ADR-0004） |
| 会话（session） | 实时模式中操作者对导轨的独占持有；失效即退回 SCHEDULED（ADR-0004） |
| 让位（yield） | 巡检守护在站间空程把导轨控制权交给实时模式的动作；该轮标记 `yielded`，退出后从下一站位续跑（ADR-0004） |
| 历史模式（history） | 巡检台的只读回溯模式：按站位与时间浏览历史图像与测量值 |
| 操作者（operator） | 现场使用巡检台的人；有权发起手动运动，须持有当前会话（ADR-0004） |
| 上位机（operator terminal） | 操作者实际看页面的那台机器：**控制室的 PC**（不是移动平板）。页面的尺寸与交互按"桌面显示器 + 现代浏览器"设计（2026-09-14 确认） |
| 图像索引（image index） | 把每帧图像的 MinIO 对象名关联到站位/时间/角度档的记录，采图时落库（ADR-0005） |
| 两段速（two-phase goto） | `Line_2Axis` 的单条指令走两段：先按**巡检档**走空程到"距目标 `approach_margin`（5 mm）"，再切**接近档**走完余程；段序由 ADR-0007 / F6 修正，真机验收见 §2.2.2 |
| 链路（link / Transport） | patrol 与外部服务之间的**唯一**出网形状：`transport(url, *, params=None, body=None) -> dict`；M1 由 `deploy.transport.HttpxTransport` 实现（ADR-0011） |
| 部署包（deploy） | 持有真实 HTTP、时钟、常驻循环的**胶水包**；patrol 保持"库内零网络调用"安全基线，二者以 `links.Transport` 协议对接（ADR-0011） |

## 部署单元

> 2026-09-14：两个工程**合并为一个仓库**（ADR-0012）后，下面的单元仍在，只是打包与部署形态
> 改为容器；`patrol` 的"库内零网络调用"边界不放宽。

- **patrol**（库房主机，Ubuntu x86_64）：FMC4030 运动 + 采图调度 + outbox；**库内零网络调用**
- **deploy**（与 patrol 同机，ADR-0011）：装配入口 `patrol-m1`——真实 `Transport`（httpx）+ 触发式/常驻循环；
  patrol 的依赖注入由它提供，跨进程边界也在这里
- **console**（与 patrol 同机，ADR-0003）：巡检台界面与手动控制服务；浏览器只与它通信
- **capture**（同机 vendored 组件）：XCloud 相机截图服务（:7003）；**用 HTTP 500 表示业务失败**（响应体仍是完整信封），
  须与链路故障区分——否则一次抖动会打断整轮
- **analysis**（prod 服务器 10.77.77.39）：环境入库 + 对齐分析 + 接收 API；`images` 表承载图像索引（ADR-0005）
- **solution**（prod 服务器，compose 项目 `mushroom_service`）：算法工程——APScheduler 调度器、
  视觉与决策分析；**巡检由它每 3 小时触发一次**（ADR-0012）

# 07 — M2 兜底：站位表 → .elo 生成与下发（可延后）

**Spec:** ../spec.md §3、§4.3

**What to build:** 生成器把站位表编译为 .elo 脚本行文件（回零 → 逐站位接近/巡检段插补 → OUT0 点灯窗口 → 循环），经 SDK `FMC4030_Download_File(type=2)` 下发并 `Start_Auto_Run`；相机侧在 OUT0 窗口内连拍/录像，事后按时间戳对齐。端到端行为：主机不可用时控制器独立巡检点灯，数据不断流。

**Blocked by:** 04 — 全库站位示教 + 一轮完整巡检（M1）

**Status:** ready-for-agent

- [ ] 站位表 → .elo 文本生成正确（指令名/参数/行号跳转与设计一致）
- [ ] WorkStudio 编译通过并实机运行：完成一轮脱机巡检 + 点灯
- [ ] SDK 下发/启动/停止脚本全流程可用（不依赖 WorkStudio）
- [ ] 相机侧窗口连拍与 OUT0 时序对齐验证（依赖相机 SD 卡计划快照支持，选型确认；不支持则本项降级，M2 只保运动）
- [ ] （可选，待厂商确认）IN0 边沿握手替代固定延时

注：兜底能力，优先级最低，可延后。

## Comments

- 2026-08-30 代码完成（commit `1d8b59b`）：`patrol/elo/generator.py`（generate 逐行生成 spec §4.3 模板脚本：前置回零 6 行 + 每站 11 行 + 收尾 3 行，接近点沿来向 5mm 链式衔接，循环回行 1；deploy 写盘→SDK 下发→启动），6 项单测。
- **待现场**：WorkStudio 编译实机验证（坐标单位/虚拟坐标系）、脱机巡检 + 点灯联调；相机 SD 卡计划快照支持仍待选型确认（不支持则 M2 降级保运动）。

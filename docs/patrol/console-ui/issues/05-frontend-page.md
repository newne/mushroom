# 05 · 前端页面落地：实时 + 历史两模式

Status: ready-for-human

## 背景

交互与信息架构已在 `.scratch/console-ui/prototype.html`（单文件、mock 数据）中定稿并逐条
验证过（45 项断言）。本票把原型接到真实 `/api`：替换 mock 适配层，其余按原型。

## 目标

- 以原型为规格实现页面：顶栏（模式开关 + 状态药丸 + 连接指示）、左栏站位（按 `box_id`
  分组）、中栏主视区、右栏操作区、底栏事件日志。
- 实时模式：`/api/status` 1s 轮询；坐标软限位在**浏览器侧**拦截；`goto`/`home` 二次确认；
  急停常驻且不随滚动隐藏；运动期间控制类按钮禁用。
- 历史模式：站位 → 时间轴（倒序，按日期分组，失败帧标记）+ 大图缩放（1×/2×/4×，拖拽平移）
  + `box_id` 生长曲线（`mean_len_mm`/`mean_cap_mm`）；筛选：日期、角度档、轮次。
- 依赖 vendor 进仓库（`console/static/vendor/`），**不走 CDN**；图表可换 ECharts/Chart.js，
  原型里的手绘 SVG 可保留为无依赖兜底。
  > **2026-09-14（ADR-0015）**：页面落在 `web/console/index.html`，故 vendor 目录相应为
  > `web/console/vendor/`；取数走**同源** `/api/...`（上机由 nginx 反代，本机 `dev.py`）。
- 深链保留：`?mode=history&station=S05`。

## 验收

- [ ] 原型里的 45 项断言在接入真实 API 后仍全部通过（把 `verify.js` 的 mock 差异点改成契约断言）。
- [ ] 1280×800 平板触控可用：所有可点元素 ≥44px，无横向滚动。
- [ ] 断网/守护重启时页面给出可理解的降级提示，不出现"点了没反应"。
- [ ] 页面上任何位置都不出现 MinIO 凭据或直连 MinIO 的 URL。

## 依赖

- 01、02、03、04；原型与验收脚本 `../prototype.html`、`../verify.js`；设计 `../spec.md`

## 备注

`verify.js` 需要 jsdom；现场无网络时把 jsdom 也 vendor 进开发环境，或用真实浏览器手工回归。

## Comments

**2026-09-14（实时模式的手动面已落地）**：`web/console/index.html` 已接上真实 `/api`，
本次补齐了**手动控制面板**（票 05 的实时部分）：

- 急停常驻右栏顶部（sticky，未接管时也可点），闩锁状态与"复位急停"分开显示；
- 接管 / 放开（放开 = 回零 + 撤权，后端排，页面把结果话说清楚）+ 会话倒计时（按**服务器
  时间**校准，上位机时钟不准也不会骗人）；
- 点动（步长 0.5/1/5/10）、定位（二次确认显示直线距离）、回零（二次确认）、补光灯、抓拍
  （绑定左栏选中站位）；越界目标在**浏览器侧**就标红并禁用提交；
- 巡检进行中提前上锁并显示"预计 x 分钟后可用"；后端 403/409 的话术**原样显示**；
- `Esc` 只中断**正在执行**的那一条（空闲时不响应：一次误按就把急停闩上、后续轮次全失败）。

回归：`docs/patrol/console-ui/verify-page.js`（jsdom，37 项断言，对着"发错就出事"的点：
载荷形状、越界拦截、二次确认、拒绝显示、Esc 边界）。跑法见文件头（需要 jsdom）。

**2026-09-15（历史模式补齐）**：顶栏加了「实时 / 历史」切换（§3 的形态），历史模式下有

- 时间轴：按日期分组、倒序、失败帧标灰并写明"采图失败"、标出来源（本地待同步 / prod）；
- 大图查看器：1×/2×/4× 按钮 + 滚轮缩放 + 放大后拖拽平移 + 「适应」复位；
- 生长曲线：SVG 两条线（平均长度 / 平均伞盖）+ 最近 8 个测量值的表格；
- 筛选：日期范围 + 角度档（角度选项只列**数据里真实存在**的档，不摆注定筛出空结果的选项）。

为此新增后端代理 `GET /api/growth?box_id=&limit=`（单一 origin；prod 不通时如实报错，
**不用空曲线顶替**——"还没有测量值"与"问不到 prod"在现场是两件完全不同的事）。

**仍然没做**：轮次筛选（要先给 prod 的 `/images` 加轮次维度）、缩略图缓存、并排两轮对比。
回归：`verify-page.js` 现在 58 项断言（新增 21 项覆盖历史模式：分组、失败帧、缩放、曲线、
筛选、prod 不通的区分）。票 05 保持 `ready-for-human`。

**2026-09-15（实时画面，ADR-0017）**：现场用了半天之后提的是"接管时看不见画面，点动只能靠
坐标猜"。加了「实时画面」卡（**中栏第一张**——点动按钮在右栏，两者要同屏；第一版放在右栏
操作卡下面是现场否掉的）：

- `<img src="/api/preview">` 直接播 MJPEG（`multipart/x-mixed-replace`），页面零播放器；
- **接管 → 自动开，放开 → 自动关**；切到历史模式也关（中栏整列换成时间轴了，留着白占相机一路）；
- **巡检开始 → 自动关**，本轮结束 → 自动恢复；预览容器不可用 → 每 2 秒重试并把后端的话
  原样显示（"预览服务连不上：…"），不写成"失败"；
- 流"卡住"（连接还在、没有新帧）浏览器不报错 → 靠上游帧龄 >3 秒自己发现并换 URL 重连；
- 页面刷新回来时，如果后端会话还在，画面自己接上（不让人以为它坏了）。

相机地址与口令**不出现在页面里**（只有 `/api/preview`）——回归里有一条静态断言专门盯这个。
**上机踩的坑**：`.pvwrap img{display:none}` 是默认隐藏，而开流写的是 `style.display=''`
（只清内联样式，规则照样生效）→ 画面真的在传却只有一个黑框，现场原话"全黑内容，并没有看到
视频流"。现在写 `display:'block'`，回归里有一条断言专盯"播放中 img 真的可见"。
回归：`verify-page.js` 现在 76 项断言（新增 18 项覆盖实时画面）。

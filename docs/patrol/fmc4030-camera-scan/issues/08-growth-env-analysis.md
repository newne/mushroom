# 08 — 生长 × 环境对齐分析：首版参数区间表 + 报表

**Spec:** ../spec.md §6

**What to build:** prod 上每日批处理：按框生长曲线重采样 → 分阶段生长速率；环境按同窗口聚合；分箱对比/分位数回归找出生长速率最高且稳定的参数区间，输出（阶段 × 参数上下限 + 置信度 + 样本量）区间表与报表 API。端到端行为：跑一次批处理，得到首版"最佳控制参数区间"报表。

**Blocked by:** 03 — 环控系统读取入库；05 — 测量管线；06 — 7×24 常驻调度

**Status:** ready-for-agent

- [ ] measurements × environment 按时间窗对齐管道可复跑
- [ ] 分阶段生长速率计算正确（含生长阶段按长度分段的定义）
- [ ] 首版最佳参数区间表产出（含置信度与样本量字段）
- [ ] 报表/API 可访问，区间表可导出
- [ ] 数据量不足的阶段明确标注"样本不足"，不输出误导性区间

## Comments

- 2026-08-30 代码完成（commit `1d8b59b`）：`analysis/align.py`（hourly_rates 按框小时级差分速率、align 生长×环境对齐、best_ranges 分位数分箱取"平均速率最高且样本 ≥3"的箱、run_from_db 批处理入口）+ API `GET /ranges`，6 项单测（48h 合成数据验证：能从 4 个温区中找回 24–26℃ 最优区间，速率 ≈0.6mm/h）。
- **待真实数据**：数据积累后跑首批处理、阶段分箱宽度（当前 20mm）整定、报表导出格式。样本不足的箱按 MIN_SAMPLES_PER_BIN=3 过滤，已满足"不输出误导性区间"。

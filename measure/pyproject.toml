[project]
name = "measure"
version = "0.1.0"
description = "测量管线：站位标定、菇体检出、mm 级参数聚合"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/measure"]

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
.kiro/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db

# Models and large binary files
models/
*.bin
*.pt
*.pth
*.ckpt
*.model
*.h5
*.joblib
*.pickle
*.pkl
*.onnx
*.safetensors
*.msgpack

# Data files
data/*.csv
data/*.jpg
data/*.png
data/*.jpeg

# Jupyter Notebooks
*.ipynb
notebooks/*.html
.ipynb_checkpoints/

# Logs
*.log
logs/
Logs/
src/Logs/*.log
mlruns/
data/
output/
dist/
.trae/
.python-version

# Temporary files
*.tmp
*.temp
.archive/
output/

# Backup files
*.backup
*.bak
*.old
*_original.*
*_backup.*
*.orig

# Sensitive configuration
src/configs/.secrets.toml
.env

# Build artifacts
dist/
build/

# PyArmor
.pyarmor/

# Cache files
*.cache
.cache/

# Project specific - Temporary documentation
PROJECT_CLEANUP_*.md
CLEANUP_*.md
GIT_*.md
LARGE_FILES_*.md
*_SUMMARY.md.tmp
*_TEMP.md
test_rendered_prompt.txt
decision_analysis_*.json

# Test files and data
test_*.py
*_test.py
test_*.json
*_test.json
*.csv
analyze_past_day_data.py
fix_imports*.py
sample_*.csv
static_config_*.csv
dynamic_results_*.csv

# Demo and temporary scripts
*_demo.py
*_temp.py

# MLflow local database
mlflow.db
build_info.json

# MLflow docker data
docker/mlflow_data/

# ---------- 巡检工程带来的忽略规则（原 mushrooms 仓库；合并见 ADR-0012） ----------
# 本地状态与工具目录
.mimosa/
.workbuddy/
docs/patrol/_ref/
# 本地 SQLite（analysis 的接收库、试跑产物）
*.db
# pytest 临时目录
.ptmp-*
# 现场部署的构建产物（离线 wheel 与源码包，可重新生成；脚本与实测取证保留）
docs/patrol/prod-deploy/wheels/
docs/patrol/prod-deploy/*.tgz
docs/patrol/prod-deploy/_m1dist/
docs/patrol/prod-deploy/__pycache__/
# 打包暂存目录（每次 tar 前现生成；已误入库过 _analysis/_m1src）
docs/patrol/prod-deploy/_*/
# 一次性现场探针脚本（_ 前缀；结论已落 gap-list）
docs/patrol/prod-deploy/_*.py
# 现场帧的分析产物与单帧派生图（可重新抓取）
docs/patrol/prod-deploy/look/
docs/patrol/prod-deploy/captures/_*
docs/patrol/prod-deploy/captures/realtime_*
# 远程执行包装（含主机密钥/口令用法，属本机工具产物）
docs/patrol/prod-deploy/_*_exec.ps1

# ---------- 巡检工程的测试必须入库 ----------
# 上面 `test_*.py` / `*_test.py` 是给算法侧的临时脚本设的，但它会连带忽略巡检的整套测试：
# 合并时实测 39 个测试文件只进去了 1 个（其余静默消失）。下面把四个包的测试目录显式收回。
# 刻意只收回 test_*.py，不用 `!dir/**` —— 那会把 __pycache__ 之类也一起收回来。
!patrol/tests/test_*.py
!deploy/tests/test_*.py
!measure/tests/test_*.py
!analysis/tests/test_*.py

# `ENV/` 是为算法侧的环境配置目录设的，但它（在大小写不敏感的路径上）连带忽略了
# analysis 的 env 子包 —— 合并时整个 analysis/src/analysis/env/ 没进去。
# 注意：目录被排除后无法再单独 re-include 里面的文件，必须把**目录本身**收回来。
!analysis/src/analysis/env/

# 冻结归档里的版本钉子（合并时被通用规则吞掉，一处收尾）
!legacy/mushroom-cls/.python-version
# 容器验证用的临时配置目录（探针产物）
docs/patrol/prod-deploy/pc-cfg/

# ---------- 算法工程的源码与测试也必须入库（2026-09-15 补） ----------
# 两条**算法侧**的老规则在同一个仓库里越界了，静默吞掉真实源码与测试：
#
# 1) 上面第 62 行的 `models/` 本意是"模型权重目录"（仓库根那份 1.5GB 的 CLIP/LLaMA 权重），
#    但它同时匹配了 `src/storage/models/` ——那是重构后的**源码包**。后果：整个包从来没进过
#    版本库，别人的克隆里 `import storage.models` 必然失败，而本机因为文件都在磁盘上，
#    一直看不出来。
# 2) 上面第 139 行的 `test_*.py` 本意是"临时测试脚本"，吞掉了 `tests/` 下 13 个真测试文件
#    （`git ls-files tests/unit` 当时只有 5 个）。巡检侧四个包之前已同样收回，算法侧这次补上。
#
# 只收回 `*.py`，不用 `!dir/**` —— 那会把 __pycache__/*.pyc 也一起收回来。
!src/storage/models/
!src/storage/models/**/*.py
!tests/**/test_*.py

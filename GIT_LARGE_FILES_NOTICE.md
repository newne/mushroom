# Git 大文件处理说明

## 问题
当前 Git 历史中包含大文件（主要是模型文件和数据文件），导致推送缓慢。

## 已排除的文件类型
- models/ 目录（605MB 的模型文件）
- data/ 目录中的 CSV 和图片文件
- Jupyter Notebooks
- 备份文件

## 解决方案

### 方案 1：使用 Git LFS（推荐）
```bash
# 安装 Git LFS
git lfs install

# 跟踪大文件类型
git lfs track "*.msgpack"
git lfs track "*.bin"
git lfs track "*.csv"
git lfs track "*.jpg"

# 提交 .gitattributes
git add .gitattributes
git commit -m "Add Git LFS tracking"
```

### 方案 2：清理 Git 历史（慎用）
```bash
# 使用 git filter-repo 清理历史
pip install git-filter-repo

# 移除大文件
git filter-repo --path models/ --invert-paths
git filter-repo --path data/ --invert-paths

# 强制推送
git push origin main --force
```

### 方案 3：创建新分支（当前使用）
```bash
# 当前提交已经排除了大文件
# 但历史中仍包含大文件
# 推送时会比较慢，但最终会成功

# 推送到远程
git push origin main --force-with-lease
```

## 当前状态
- ✅ .gitignore 已更新，排除所有大文件
- ✅ 当前工作目录中的大文件已从 Git 跟踪中移除
- ⚠️ Git 历史中仍包含大文件（约 600MB）
- 📝 首次推送会比较慢，后续推送会很快

## 建议
1. 首次推送完成后，后续提交不会再包含大文件
2. 如果需要彻底清理历史，使用方案 2
3. 如果团队需要共享模型文件，使用 Git LFS（方案 1）
